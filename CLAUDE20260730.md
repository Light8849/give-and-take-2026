# CLAUDE.md — Give&Take【Max】引き継ぎ書

このリポジトリは流通経済大学付属柏高校の**授業交換Webアプリ「Give&Take【Max】」**。
教員同士が授業を交換・移動した記録を管理し、カレンダーで全教員に共有する。
公開URL: https://light8849.github.io/give-and-take-2026/ （GitHub Pages）

現行バージョン: **v5.8**（2026-07-30、萩原先生の育休復帰対応）

## アーキテクチャ

- **index.html 1ファイル構成**。HTML/CSS/JS/データすべてがこの1ファイルに入っている。ビルド工程なし。
- 基本時間割データは `<script id="jdata" type="application/json">` に埋め込まれたJSON（約450KB）。
- 交換履歴は **Google Apps Script (GAS) 経由で Googleスプレッドシート**に読み書きする。
- ページロード時: jdata（基本時間割）+ GASから取得した交換履歴 → カレンダー描画。

## ⚠️ 絶対に壊してはいけないもの

UI刷新はCSS・レイアウト・配色・UXの変更が目的。**以下のロジックとデータは変更禁止**:

1. **jdata のJSON構造と内容**（entries / teachers / classes / teacher_off_days / teacher_blocked / teacher_meetings）。キー名・値の形式を一切変えない。
2. **GAS連携**: GAS URLの定数、gasGet/gasPost系の関数、送受信するパラメータ名。
3. **スプレッドシートの列構造との互換性**（下記参照）。書き込み形式を変えると過去の全履歴が読めなくなる。
4. **交換ロジック**: searchSwap（交換候補検索）、submitSwap、三角交換、undoSwap（取り下げ）、手差し移動モード（type:'move'）。
5. **取り下げのマッチング方式**: teacherA + teacherB + dateA + dateB の4項目一致。datetime一致に戻さない（過去にバグ化した）。
6. **クラス占有チェック**: 交換候補検索時、対象クラスに別授業がある枠は無条件除外。
7. **パスワード認証の仕組み**（値はコード内の定数参照。UIに平文表示しない — 過去にバージョン表記から意図的に削除した経緯あり）。
8. **祝日データ**（2027/3まで定義済み）と土曜3限制（平日8限・土曜3限）。

## データ構造の要点

**jdata.entries**（1427件 = 週間の全授業コマ）:
```json
{"pcode":"P0333","day":1,"period":1,"class":"C000000027","class_name":"3-8",
 "lesson":"数学Ⅲ4","subject":"数学","teachers":["萩原"],
 "teacher_codes":["T000000073"],"all_classes":["3-8"],"is_deploy":false}
```
- day: 1=月〜6=土 / period: 1〜8（土曜は1〜3）
- 同一授業の週複数コマは同じpcodeを共有
- teachers が複数 = 展開授業（例: 選数C/選国 は熊田/萩原の2人）

**スプレッドシート列**（交換履歴、1行=1件）:
| 列 | 内容 | 注意 |
|---|---|---|
| A | 日時 `2026-05-13 10:00:00` | |
| B | クラス | **先頭に ' を付けて書き込む**（日付自動変換対策。"Mon Ja"問題） |
| C-E | 教員A / 授業A / 元曜日時限A | |
| F-H | 教員B / 授業B / 元曜日時限B | |
| I-L | dateA / periodA / dateB / periodB | |
| M | type | 空=交換、'move'=手差し移動 |

## 過去に潰したバグ（再発させない）

- **授業増殖**: addToSwapMapで同一 date+teacher+period は上書き（重複追加しない）
- **別週への波及**: isSlotSwappedAway / clickCell は dateStr 引数で日付スコープ化済み
- **GAS URL変更**: GASを再デプロイするとURLが変わる。定数の更新を忘れると dateA/periodA が空で送信される
- **submitSwap/closeSwap後は renderCal() を必ず呼ぶ**（画面反映漏れ）

## 既知の制限（仕様として許容中）

- 過去日の授業交換はUIからは不可（getNextDates が今日以降のその曜日しか返さない）。必要時はスプレッドシートに直接行を追加して対応（上記列構造どおり。B列の ' を忘れない）。

## 変更時のルール

- 画面内のバージョン表記を更新する: 形式 `v5.X YYYY-MM-DD HH:MM`。UI刷新なら v6.0 に上げてよい。
- コンソールログは本番に残さない（過去に全削除済み）。
- 動作確認は「閲覧パスワードでカレンダー表示 → 交換検索 → （テスト日付で）交換登録 → 取り下げ → 手差し移動」の一連が通ること。スプレッドシートに正しい形式で行が増えることも確認。
- 教員数108・クラス41（高校29+中学12）・週1427コマ。カレンダーは日付単位表示で、交換済みコマ・手差しコマ（黄色📌）の視覚区別がある。この情報量が崩れないデザインにすること。

## ユーザー（利用者）像

- 全教員がスマホ/PCで閲覧。教務担当が交換・確定・移動を操作。
- 学校現場なので、派手さより**視認性・誤操作しにくさ・印刷しても分かる配色**を優先。
